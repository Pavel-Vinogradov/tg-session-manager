server:
  port: ${SERVER_PORT:8080}
  host: ${SERVER_HOST:localhost}
  grpc_port: ${GRPC_SERVER_PORT:50051}

telegram:
  default_api_id: ${TELEGRAM_API_ID}
  default_api_hash: ${TELEGRAM_API_HASH}
  session_dir: ${SESSION_DIR:./sessions}

log:
  level: ${LOG_LEVEL:info}
  json: false
